# PRD: 엑셀 통합 정리 웹서비스 (excelsum)

## 1. 개요

동일한 양식(서식)을 사용하지만 내용(값)만 다르게 채워진 여러 개의 엑셀 파일을 웹페이지에 업로드하면, 이를 자동으로 취합·정리하여 하나의 엑셀 파일로 다운로드할 수 있는 웹서비스를 만든다.

예: 여러 지점/팀/개인이 동일한 양식의 보고서, 신청서, 실적표를 각자 작성해서 보내오면, 담당자가 이를 일일이 복사·붙여넣기로 합치는 대신 이 서비스에 파일들을 한 번에 올려서 통합본을 받는다.

## 2. 배경 및 문제 정의

- 동일 서식의 엑셀 파일을 여러 명(또는 여러 부서)이 각자 작성해 제출하는 업무가 반복적으로 발생한다.
- 담당자는 이 파일들을 열어서 수작업으로 하나의 시트에 복사·붙여넣기하며 취합하고 있어, 시간이 오래 걸리고 실수(누락, 중복, 셀 밀림)가 발생하기 쉽다.
- 파일 수가 많아질수록(수십~수백 개) 수작업 취합은 사실상 불가능에 가까워진다.

## 3. 목표 (Goals)

- 사용자가 동일 서식의 엑셀 파일 여러 개를 한 번에 업로드할 수 있다.
- 서비스가 각 파일에서 데이터 영역을 인식하여 하나의 통합 엑셀 파일로 정리한다.
- 통합 결과를 즉시 다운로드할 수 있다.
- 서식이 다르거나 인식할 수 없는 파일이 섞여 있을 경우 사용자에게 명확히 알려준다.

## 4. 비목표 (Non-goals) — 1차 버전 범위 제외

- 서로 다른 서식(레이아웃이 다른 양식)을 자동으로 매핑·통합하는 기능
- 엑셀 외 포맷(CSV 제외, PDF, 이미지 등) 지원
- 파일 간 데이터 유효성에 대한 비즈니스 로직 검증(예: 금액 검증, 승인 워크플로우)
- 사용자 계정/로그인 기반 파일 이력 관리 (1차 버전은 비로그인 1회성 처리로 가정)

## 5. 타겟 사용자

- 여러 사람/부서로부터 동일 양식의 엑셀 파일을 취합해야 하는 담당자(총무, 인사, 영업관리, 교육 담당자 등)

## 6. 핵심 사용자 시나리오

1. 사용자가 웹페이지에 접속한다.
2. "파일 업로드" 영역에 동일 서식의 엑셀 파일 여러 개를 드래그 앤 드롭하거나 선택하여 올린다.
3. 서비스가 업로드된 파일들의 서식(헤더 구조)이 서로 일치하는지 확인한다.
4. 문제가 없으면 각 파일의 데이터 행을 추출하여 하나의 시트로 병합한다.
5. 병합 결과 미리보기(표 형태)를 화면에 보여준다.
6. 사용자가 "다운로드" 버튼을 눌러 통합된 엑셀 파일(.xlsx)을 받는다.
7. 만약 서식이 다른 파일이 섞여 있다면, 어떤 파일이 문제인지와 어떤 부분이 다른지를 사용자에게 안내한다.

## 7. 기능 요구사항

### 7.1 파일 업로드
- 다중 파일 업로드 지원 (드래그 앤 드롭 + 파일 선택 버튼)
- 지원 포맷: `.xlsx` (필요 시 `.xls`, `.csv` 추가 검토)
- 파일 개수 및 개별 파일 용량 제한 설정 (예: 최대 50개, 파일당 10MB — 추후 조정 가능한 값으로 설계)
- 업로드된 파일 목록과 각 파일의 상태(정상/오류) 표시

### 7.2 서식 인식 및 검증
- 헤더 행(컬럼명)을 기준으로 각 파일의 서식이 서로 동일한지 비교한다.
- 헤더 행 위치는 고정(예: 항상 1행) 또는 사용자가 지정 가능하도록 한다.
- 서식이 일치하지 않는 파일 발견 시:
  - 통합 대상에서 제외하고 목록에서 "서식 불일치"로 표시
  - 어떤 컬럼이 다른지(누락/추가/순서 다름) 구체적으로 안내

### 7.3 데이터 병합
- 각 파일에서 헤더를 제외한 데이터 행만 추출하여 하나의 시트에 순서대로 이어붙인다.
- 원본 파일명 또는 식별자를 별도 컬럼(예: "출처 파일")으로 추가하여, 통합 후에도 어느 파일에서 온 데이터인지 추적 가능하게 한다.
- 빈 행, 완전 공백 행은 자동으로 제외한다.
- 병합 순서는 업로드 순서(또는 파일명 정렬)를 기본값으로 한다.

### 7.4 결과 확인 및 다운로드
- 병합 결과를 웹 화면에서 표 형태로 미리보기 제공
- 통합 파일을 `.xlsx` 형식으로 다운로드
- 다운로드 파일명은 자동 생성(예: `통합결과_YYYYMMDD_HHMM.xlsx`)

### 7.5 오류 및 예외 처리
- 손상된 파일, 암호로 보호된 파일 업로드 시 명확한 오류 메시지 표시
- 서식이 다른 파일에 대한 안내 및 재업로드 유도
- 데이터가 하나도 없는 경우(모든 파일이 헤더만 존재) 안내 메시지 표시

## 8. 비기능 요구사항

- **성능**: 파일 수십 개(합산 수만 행 수준)를 업로드해도 수 초~수십 초 내 처리 완료
- **보안**: 업로드된 파일은 처리 후 서버에 영구 저장하지 않음(처리 완료 또는 세션 종료 시 삭제) — 민감 정보가 포함될 수 있는 업무 파일 특성 고려
- **브라우저 호환성**: 최신 크롬/엣지 기준 정상 동작
- **접근성**: 파일 업로드, 오류 메시지 등 스크린리더로 인지 가능한 수준의 기본 접근성 확보
- **반응형 지원**: PC 환경을 기본(주) 사용 환경으로 설계하되, 모바일 브라우저에서도 핵심 흐름(업로드, 상태 확인, 미리보기, 다운로드)이 깨지지 않고 동작하도록 반응형 레이아웃을 적용한다.
  - PC: 다중 파일 드래그 앤 드롭, 넓은 화면에서의 표 미리보기 등 풀 기능 제공
  - 모바일: 화면 폭에 맞춘 레이아웃 전환(파일 목록 카드형 배치, 미리보기 테이블 가로 스크롤 또는 축약 뷰 등), 파일 선택 버튼을 통한 업로드 지원(모바일 브라우저의 드래그 앤 드롭 제약 고려)

## 9. 데이터 처리 상세 (구현된 동작)

- **헤더 행 위치**: 1행 고정이 아니라, 각 시트의 처음 20행 안에서 "서로 다른 값이 2개 이상인 첫 행"을 헤더로 자동 인식한다. 제목 행(빈 줄, 병합된 제목 셀 등)이 앞에 있어도 정상 인식된다.
- **여러 시트 처리**: 한 파일에 시트가 여러 개면 순서대로 훑는다. 처음 찾은 헤더를 그 파일의 기준 서식으로 삼고, 같은 헤더를 가진 뒤쪽 시트의 데이터는 이어붙인다. 표 형태가 아니거나(표지, 안내문 등) 헤더가 다른 시트는 데이터에서 제외하고 "제외된 시트: OO" 형태로 안내한다.
- **병합 셀**: 헤더 행 인식 시, 병합된 제목 셀이 전체 칸에 같은 값으로 채워져 나오는 ExcelJS 특성을 고려해 "채워진 칸 수"가 아니라 "서로 다른 값의 개수"로 판단하므로 오인식하지 않는다.
- **컬럼 순서**: 파일 간에는 순서가 동일해야 한다(순서가 다르면 "서식 불일치"로 분류). 한 파일 내 여러 시트 간에도 동일하게 적용된다.
- **서식 유지**: 다운로드되는 통합 결과는 가장 먼저 업로드된 파일의 헤더 스타일(글꼴/배경/테두리), 열 너비, 데이터 숫자 서식을 그대로 따른다.

## 10. 기술 스택 제안

- **프론트엔드**: 웹 브라우저에서 파일 업로드 UI 및 미리보기 제공 (React/Next.js 등)
- **엑셀 파싱/생성**: `SheetJS(xlsx)` 또는 `ExcelJS` 라이브러리 활용
- **처리 위치**: 파일 용량과 보안 요구사항을 고려하여 브라우저 내 처리(서버 미경유) 우선 검토, 대용량/서버 검증이 필요할 경우 백엔드 처리로 전환

## 11. 성공 지표

- 담당자가 파일 취합에 들이는 시간이 기존 대비 크게 단축된다(예: 수작업 대비 90% 이상 시간 절감).
- 서식 불일치로 인한 통합 오류(잘못 합쳐진 데이터) 발생률 0%에 근접.

## 12. 오픈 이슈

1. 프론트엔드 전용(브라우저 처리)으로 결정, 서버 없이 정적 배포됨 — 해결됨
2. 파일 최대 개수/용량 제한: 50개, 파일당 10MB로 확정 (`MAX_FILES`, `MAX_FILE_SIZE_BYTES`)
3. 서식 불일치 판정 기준: 컬럼명 집합과 순서가 완전히 같아야 "정상"으로 간주(엄격 일치). 유사 판정(오타/동의어 허용)은 1차 범위 밖.
